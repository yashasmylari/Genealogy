


Person {
	String name;
	String gender;
	String firstName;
	String lastName;
	String dateOfBirth;
	String placeOfBirth;
	Boolean isAlive;
	String dateOfDeath;
	String region;
	String language;
	String religion;
	String clan;
	String ethnicity;
	String occupation;
	String physicalTraits;
	String education;
	String medicalCondition;
	String specialCharacteristic;
}



// Working on REGION to make a node connecting to the Person node as per Barabara's Suggestion


// User Story #1 (Vivek)
// Done Using API need to confirm whether it is Ok




// User Story #2
MATCH (p1:Person {name: "Roy"}), (p2:Person {name: "Jack"}),
path = shortestpath((p1)-[:FATHER_OF | :MOTHER_OF |:MARRIED_TO *]-(p2))
RETURN path




// User Story #3
MATCH (k:Person)-[r:FATHER_OF | MOTHER_OF *1..7 ]-(l:Person)
WHERE k.name = "Rita" AND SINGLE(x IN l.medicalCondition WHERE x = "Asthma")
RETURN k,r,l


// User Story #4 
// Migration Country name
MATCH (k:Person)-[r:FATHER_OF | MOTHER_OF *1..7 ]-(l:Person)
WHERE k.name = "Jason Smith" AND
	ANY (x IN k.locations WHERE x =~ ".*Germany.*" /*"(?i).*Germany.*" IN x*/ )
RETURN k, r, l,  k.locations

// Reason for Migration
// Medical Treatment
MATCH (k:Person)-[r:FATHER_OF | MOTHER_OF *1..7 ]-(l:Person)
WHERE k.name = "Jason Smith" AND
	ANY (x IN k.locations WHERE x =~ ".*Education.*" /*"(?i).*Germany.*" IN x*/ )
RETURN k, r, l,  k.locations



// User Story #5
// In Progres






// To find the relationship
MATCH (n:Person)
WHERE single(x IN n.medicalCondition WHERE x = "Asthma")
RETURN n

